#include"data.h"
#include"view.h"
#include"control.h"
#include"AI.h"


int main()
{
	show(a);
	while (1)
	{
		char ch = getchar();
		getchar();
		moveche(a, ch);
		show(a);


	}


	findway(a, 0, 0);
	show(b);
	goout();
	show(a);










	system("pause");
}

